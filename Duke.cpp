#include "Duke.hpp"

namespace coup{
    /**
     * @brief Duke source file (to be implemented)
     * functions documnatation is found in Duke header file 
     * 
     */
    Duke::Duke(Game& _game, const std::string& player_name) : Player(_game,player_name) {}

    void Duke::block(Player& b) {}
    void Duke::tax() {}
}